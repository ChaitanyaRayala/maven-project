pipeline{
    agent any
    tools{
        maven 'localMaven'
        jdk 'localJdk'
    }
    //To specify IP addresses for aws tomcat instances
    parameters{
        string(name:'tomcat_dev',defaultValue:'34.227.162.42',description:'staging server')
        string(name:'tomcat_production',defaultValue:'54.175.78.150',description:'production server')
    }
    
    //Run the jenkins job at given time conditions
    triggers{
        pollSCM('* * * * *')
    }

    stages{
        stage('Build'){
            steps{
                bat 'mvn clean install'
            }
            post{
                success{
                    echo 'Now archiving..'
                    archiveArtifacts artifacts:'**/target/*.war'
                }
                failure{
                    echo 'cannot copy artifacts to jenkins'
                }
            }
        }
        stage('Deployment'){
            parallel{
                stage('Deploy to staging'){
                    steps{
                        bat "winscp scp:ec2-user@{params.tomcat_dev}:/var/lib/tomcat7/webapps /privatekey="C:/Users/Rayala/Desktop/Chaitanya/AWS/tomcatPrivateKey.ppk" /upload **/target/*.war"
                    }
                }
                stage('Deploy to production'){
                    steps{
                        bat "winscp.exe /console -i C:/Users/Rayala/Desktop/Chaitanya/AWS/tomcat.pem **/target/*.war ec2-user@${params.tomcat_production}:/var/lib/tomcat8/webapps"
                    }
                }
            }
        }
    }
}